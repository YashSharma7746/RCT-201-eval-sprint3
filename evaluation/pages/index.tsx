import Head from 'next/head'
import { Inter } from '@next/font/google'
import { Box, Image, Text, Button } from "@chakra-ui/react";
import Link from 'next/link';
import Projects from './projects';
const inter = Inter({ subsets: ['latin'] })

type userObject = {
  avatar_url: string,
  bio: string,
  login: string
}
let skills = ["TYPESCRIPT ","REACTJS ","HTML ",'CSS ','JAVASCRIPT ','CHAKRA UI ','NEXT JS ']
let education = ["Full Stack Web Developer","Bachelor Of Commerce", "Higher Seconday"]
export default function Home(data:userObject) {

  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Box display={"flex"} justifyContent="space-between" p="20px">
        <Box w="25%" textAlign={"center"} justifyContent="center" alignItems={"center"}>
          <Box boxShadow={"xl"} rounded="md" justifyContent={"center"} p="15px" textAlign={"center"} alignItems="center">
            <Box display={"flex"} justifyContent="center">
              <Image w="10rem" borderRadius={"50px"} src={data.avatar_url}/>
            </Box>
            <Link href="https://github.com/YashSharma7746" target="_blank" rel="noopener noreferrer">@{data.login}</Link>
            <Text>{data.bio}</Text>
            <Button colorScheme={"blue"} mr="10px"><Link href="https://drive.google.com/file/d/1-27JqVP743yK-A3ZE3CvIRpF32OzsQfL/view?usp=sharing" target="_blank" rel="noopener noreferrer">Resume</Link></Button>
            <Button colorScheme={"green"}>Follow</Button>
          </Box>
          <Box boxShadow={"xl"} rounded="md" p="15px">
            {skills.map((skill:string)=>{
              return <Box key={skill} mt="20px"><Text backgroundColor={"teal.100"} p="3px">{skill}</Text></Box>
            })}
          </Box>
          <Box boxShadow={"xl"} rounded="md" p="15px">
            {education.map((education:string,i:number)=>{
              return <Text marginTop={"15px"} backgroundColor={"teal.100"} p="4px" key={i}>{i+1}. {education}</Text>
            })}
          </Box>
        </Box>
        <Box>
          <Projects />
        </Box>
      </Box>
    </>
  )
}

export async function getStaticProps(){
  let res = await fetch("https://api.github.com/users/YashSharma7746");
  let data = await res.json();
  return {
    props : data
  }
}